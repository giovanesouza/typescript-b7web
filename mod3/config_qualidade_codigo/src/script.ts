let nome: string = 'Giovane';

function somar(n1: number, n2: number): number {
    return n1 + n2;
}


somar(10, 20);


/*
"noUnusedLocals": true,  // Avisa quando tiver uma variável que foi criada e não está sendo utilizada - nível local.
"noUnusedParameters": true,  // Avisa quando tiver um parâmetro que não está sendo utilizado.
"noImplicitReturns": true,  // Opcional - relacionado a retornos de funções

*/ 